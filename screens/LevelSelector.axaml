<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:svg="clr-namespace:Avalonia.Svg.Skia;assembly=Avalonia.Svg.Skia"
        x:Class="AbiturEliteCode.LevelSelector"
        Title="Level Wählen"
        Width="450" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="#202124"
        SystemDecorations="BorderOnly"
        KeyDown="Window_KeyDown">

	<Border CornerRadius="8" Background="#202124" BorderBrush="Transparent" BorderThickness="1">
		<Grid RowDefinitions="Auto, *, Auto" Margin="15">

			<!-- header -->
			<Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Margin="0,0,0,15">
				<!-- title -->
				<StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
					<TextBlock Name="TxtTitle" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
					<!-- badge level counter -->
					<Border Name="CountBadge" Background="#2D2D30" CornerRadius="12" Padding="10,5" VerticalAlignment="Center">
						<TextBlock Name="BadgeText" Foreground="#32A852" FontWeight="Bold" FontSize="14" />
					</Border>
				</StackPanel>

				<!-- search / code input container -->
				<Border Name="SearchContainer" Grid.Column="1" Margin="15,0" />

				<!-- right header panel -->
				<StackPanel Name="HeaderRightPanel" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" />
			</Grid>

			<!-- body -->
			<ScrollViewer Name="ContentScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" />

			<!-- footer -->
			<Grid Grid.Row="2" ColumnDefinitions="*, Auto" Margin="0,15,0,0">
				<Button Name="BtnToggleMode" Grid.Column="0" HorizontalAlignment="Left" Padding="15,10" CornerRadius="4" Background="#2D2D30" Foreground="White" Margin="0,0,10,0">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<svg:Svg Name="IconToggleMode" Path="/assets/icons/ic_folder_custom.svg" Width="18" Height="18" />
						<TextBlock Name="TxtToggleMode" Text="Eigene Levels" VerticalAlignment="Center" />
					</StackPanel>
				</Button>

				<Button Name="BtnClose" Grid.Column="1" Content="Schließen" HorizontalContentAlignment="Center" Padding="15,10" CornerRadius="4" Background="#3C3C3C" Foreground="White" />
			</Grid>
		</Grid>
	</Border>
</Window>
